<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Games</title>
  </head>
  <body>
    <h1>Games</h1>
    <% games.forEach(game => {%>
        <%= game.name %>
        <%= game.genre %>
        <%= game.publishedYear %>
        <%= game.status %>
   <% }); %>

  </body>
</html>